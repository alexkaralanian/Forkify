{
  "name": "forkify",
  "version": "1.0.0",
  "description": "recipe app",
  "scripts": {
    "start": "parcel index.html",
    "prebuild-dev": "shx rm -rf dist/*",
    "build-dev": "parcel build index.html --no-optimize --public-url ./",
    "prebuild": "shx rm -rf dist/*",
    "build": "parcel build index.html --public-url ./",
    "push-gh-pages": "push-dir --dir=dist --branch=main --cleanup --verbose"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexkaralanian/Forkify.git"
  },
  "author": "Alex Karalanian",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/alexkaralanian/Forkify/issues"
  },
  "homepage": "https://github.com/alexkaralanian/Forkify#readme",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "gh-pages": "^6.2.0",
    "parcel": "^2.12.0",
    "shx": "^0.3.4"
  },
  "dependencies": {
    "core-js": "^3.38.1",
    "fractional": "^1.0.0",
    "regenerator-runtime": "^0.14.1"
  }
}
